## Tasks

- [x] 1. Add failing tests for explicit `priority` Platform Responses and compact requests forwarding `default`.
- [x] 2. Add failing test coverage for API-key-enforced `priority` on Platform forwarding `default` while preserving requested-tier logs.
- [x] 3. Update Platform service-tier forwarding helpers and call sites so every Platform upstream payload uses `default`.
- [x] 4. Run targeted tests and OpenSpec validation.
